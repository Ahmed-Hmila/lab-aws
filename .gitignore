# Terraform - NE JAMAIS committer
.terraform/
.terraform.lock.hcl    # Attends, NON : on veut le COMMITTER → on l'exclut pas

# États Terraform - TRÈS sensible
*.tfstate
*.tfstate.*
*.tfstate.backup

# Variables réelles - ne pas committer
*.tfvars
*.auto.tfvars

# Mais on autorise l'exemple
!terraform.tfvars.example

# Lock file : on le COMMIT (important pour reproductibilité)
!.terraform.lock.hcl

# Autres
.DS_Store
*.log