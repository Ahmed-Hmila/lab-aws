# Terraform state files
*.tfstate
*.tfstate.backup

# Crash logs
crash.log

# Local .terraform directories (providers, plugins, etc.)
.terraform/

# Terraform variable files (optionnel, si sensibles)
*.tfvars

# Terraform lock file (optionnel, mais peut être versionné)
# .terraform.lock.hcl

# OS / IDE files
*.swp
*.vscode/
*.idea/
*.DS_Store
